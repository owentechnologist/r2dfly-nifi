#!/usr/bin/env bash
# Single source of truth for this project's version string. Sourced by every top-level script
# (not the *-lib.sh helper libraries, which are never run directly) so `--version`/startup output
# stays in sync without hunting down the string in more than one place.
R2DFLY_VERSION="r2dfly-A-012"
